name: Gradle Publish Boot Image

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
      - '[0-9]+.[0-9]+.[0-9]+-rc.[0-9]+'

jobs:
  call-gradle-publish-boot-image:
    uses: ./.github/actions/gradle-publish-boot-image.yml
    with:
      version: ${{ github.ref_name }}